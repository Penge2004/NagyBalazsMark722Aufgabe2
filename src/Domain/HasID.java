package Domain;

/**
 * The HasID interface represents a contract for classes that have a unique identifier (ID).
 * It provides a method to retrieve the ID of an object.
 */
public interface HasID {
    /**
     * Gets the unique identifier (ID) of the object.
     *
     * @return the unique identifier of the object
     */
    int getID();
}
